import { Box } from "@mui/material";
import React, { useState } from "react";

interface LinkPreviewProps {
  url: string;
}

const LinkCard: React.FC<LinkPreviewProps> = ({ url }) => {
  return <Box>Link Card</Box>;
};

export default LinkCard;
